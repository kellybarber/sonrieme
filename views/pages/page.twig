{% extends 'layouts/base.twig' %}
{% block content %}
    <div class="blog-post">
        <h2 class="blog-post-title">{{ post.title }}</h2>
        {{ post.content | shortcodes }}
    </div>
{% endblock %}