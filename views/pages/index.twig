{% extends 'layouts/base.twig' %}
{% block content %}
  <div>
    {% for post in posts %}
      <div>
          <h1>{{ the_title }}</h1>
          <p>{{ the_content }}</p>
      </div>
    {% endfor %}
  </div>
{% endblock %}